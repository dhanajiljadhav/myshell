#!/bin/bash

str=
if [ -z $str ]
then
   echo "string is empty"
   else
     echo "string non empty"
     fi

     if [ !-z $str ]
       then
        echo "empty str"
	else
	  echo "Non empty string"

	  fi

	  if [ -n $str ]
	  then
	    echo "zero size"
	    else
	     echo "non empty"
	     fi
